{
  "mcpServers": {
    "mem-vault": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/server/index.js",
        "mcp"
      ],
      "env": {
        "MEM_VAULT_ROOT": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
